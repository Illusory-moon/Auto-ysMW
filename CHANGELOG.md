# 更新日志（Changelog）

本项目的显著变更记录。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [1.1.0] - 2026-09-06

### 新增
- **B服（Bilibili 服）自动识别与适配**：启动时按右下角 UID（>500_000_000 即 B服）自动判定服务器，区分官服 / B服两套配置。
- **服务器配置档案**：`OFFICIAL` / `BILI` 两套参数集中管理（奇域编号、历史目标名、历史识别框、进入按钮、局内行走、退出判定）。
- **首次/后续进入按钮差异化**：官服点「开始游戏」（START_GAME），B服点「单人挑战」（SOLO_BOX）。「前往大厅」位置不变。
- **历史列表按名字定位**：目标奇域不一定在列表最上方，OCR 按名字定位后点击。
- **管理删除列表按名字定位**：要删除的关卡不一定在列表最上方，按名字定位后点击其选中框（x 用选中框位置，y 用文字中心往下 20px）。
- 新增 OCR 辅助：`wait_any_text`（区分状态）、`locate_text`（返回窗口相对坐标框）、`read_uid`（提取 UID）。

### 修复
- 恢复 **Alt+N 未识别时的兜底策略**：15s 超时后兜底静默 5s 继续按 B，不再因识别不到而中止整个周常（B服可能不显示该提示）。
- **B服局内无需识别「退出奇域」**：吃满金币无提示自动结算，W 过后直接识别「返回大厅」。

### 优化
- 从**大世界进入**时自动处理「前往大厅 → 加载动画 → 进入按钮」的过渡，不再要求必须在千星奇域大厅开局。

## [1.0.0] - 2026-09-02

### 新增
- 首个可用版本：GUI + OCR 窗口识别 + 周常 16 次循环自动刷经验。
- 支持从搜索/历史进入，完成按 F1 展示经验界面。
